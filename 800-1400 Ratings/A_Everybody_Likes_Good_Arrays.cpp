#include <bits/stdc++.h>
using namespace std;

void solve()
{
    int n;
    vector<int>v(n);
    for(int i = 0;i<n;i++)
    {
        cin>>v[i];
    }
    int count = 0;
    while(i++)
    {
        if(a[i])
    }
}
int main ()
{

    int t;
    cin>>t;
    while(t--)
    {
        solve();
    }
    return 0;
}